{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apega-admin && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd apega-admin && node server.js"
  }
}
